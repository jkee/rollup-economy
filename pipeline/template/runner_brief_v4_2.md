# v4.2 isolated research-runner brief

Research exactly one manifest entry with no delegation. Use only the research
model route bound by that entry. The entry and execution envelope must use the
neutral kind `target`; never infer a route from the industry.

Before any model call, verify the manifest-bound prompt, industry specification,
normalized dataset, methodology, and packet schema exist and match every pinned
path, byte length, and SHA-256. Stop if an authority or required specification
is absent or mismatched. Read only those files. Do not seek orchestration
metadata, expected outcomes, prior economic artifacts, another runner's packet,
or any prior review.

Substitute only the prompt's four exact runtime fields: model ID, date, run ID,
and prompt version. Each placeholder must occur exactly once before
substitution, and prompt version must be `v4.2-target-archetype-1`. The same
outcome-neutral prompt bytes are used for every route.

Write one packet to a private temporary repository path, validate it there,
then invoke `pipeline/build/guarded_write_v4_2.py` for artifact kind `packet`
with the exact task path; feed the claim token supplied in this fork-none task
message through an anonymous FIFO or Unix-socket FD. Regular files and
directories are forbidden credential endpoints. Never place the token in a repository file,
envelope,
artifact, command argument, log, or response. Direct writes to the planned
packet path are forbidden. The guarded writer alone may create that path.
If the process is interrupted after acquiring its reservation lock, rerun the
same command with `--recover` and the same private capability FD (no source is
required). Recovery accepts only the exact lock-only state or an exact
identity-valid output-plus-lock state; it restores the reservation or completes
the receipt respectively, and refuses conflicting bytes without mutation.
After the packet claim is consumed, report completion to root; root runs the
deterministic finalizer into private temporary record/memo paths and separately
consumes their finalizer-owned claims. Never author or edit scores, intervals,
readiness, h, gates, actions, verdicts, finalized records, or rendered memos.

Preserve the frozen archetype, controllable-value-added boundary, documented
pass-through categories, distinct cash-cost identities, roles, and weights.
For an `assumption` value basis, preserve the pre-frozen source-free role
taxonomy and unit-sum weights; attach no B evidence, never substitute the
normalized-dataset role mix, require bounded `ASSUMPTION`/`NONE` packet cash
costs and removable fractions with empty source IDs, and force readiness
`UNPROVEN`.
Keep V removability, I realization and investment, C commercial retention, B
sale availability, access/win, deal execution and integration capacities, P
price/multiple resilience, and fixed-base CVA survival separate. Require the
closed `ACCRUAL_RECOGNIZED_REVENUE` / `TRAILING_TWELVE_MONTHS` accounting-basis
object and structured pass-through partition; typed `DEAL_EXECUTION` and
`INTEGRATION_ONBOARDING` capacity derivations with closed horizon, calculation,
and scope; canonical entry-equivalent six-dimension P state/digest; the
digest-bound `ENTRY_EQUIVALENT_COMPARABLE_MULTIPLE_LEVEL` anchor basis; closed
nonpositive adjustments; and an exhaustive Laspeyres service basket whose
baseline quantity, revenue, and pass-through are each point-identified with
low = base = high. Missing primitives stay missing;
ASSUMPTION and MISSING make readiness UNPROVEN without implying economic
quality.

Validate packet schema, runtime and manifest identity, source closure,
recognized-revenue accounting basis, independent-validation source-ID
disjointness and exact `validation_source_ids_disjoint_from_primary_support`
independence basis,
primitive per-year ranges and unconstrained-across-years realization paths,
all pinned digests, fresh finalization, and
byte-identical memo rendering before reporting completion. Attempt 2 is
permitted only when the manifest binds it to an exact schema-valid `reject`
review whose material findings name remediable primitive paths. `invalid` and
fatal-mechanics reviews are not remediable. There is no third attempt.
