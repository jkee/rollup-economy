# Research one industry for the AI roll-up screen — NAICS 541519, Other Computer Related Services (US)

**Template version: 3.1.1** — atomic-fact and explicit-method evidence contract. The frozen formulas, gates and verdict thresholds are unchanged. The assigned runtime model ID is `{{MODEL_ID}}`; copy that exact ID into `run_meta.model_id`.

Work context-free. Do not inspect existing scoring, drafts, runs, reviews, golden outcomes, generated site data or deep-dives. If live web access is unavailable, stop. Never research from memory and never fabricate a URL, quote, figure or scope.

## What you author

Write one JSON object conforming exactly to `pipeline/build/schemas/research_draft_v3_1_1.schema.json`. You author reusable atomic `evidence_facts` and selected research/judgment inputs. You do **not** author final provenance, deterministic dataset values, role weights, role contributions, factor scores, `S`, uplift or a verdict. Plain Python owns those fields.

Every selected input has a `method`:

- `OBSERVED` — `value` repeats a value stated by a cited fact exactly. Include the identical `observed_value`. No interpretation, proxy, normalization or judgmental bridge is permitted.
- `CALCULATED` — `value` is fully reproducible arithmetic over named numeric operands, every operand is backed by a cited fact, and the expression uses only numeric constants, parentheses and `+ - * /`. Include every operand's `name`, `value` and `fact_id`. Python recomputes the result. Any judgmental mapping makes this method invalid.
- `ESTIMATE` — required whenever any bridge from facts to the selected value is judgmental, including proxy choice, population mapping, normalization assumptions or an opinion. Its structured `estimate_basis` must enumerate every starting fact ID, every scope mismatch, each ordered proxy/normalization/population step, and the explicit judgmental step reaching the selected value. A cited background fact does not turn an estimate into `OBSERVED` or `CALCULATED`.

Python maps `OBSERVED → DIRECT`, `CALCULATED → DERIVED`, and `ESTIMATE → ESTIMATE`. Never author `provenance_type` in the draft. `evidence_quality` is a separate reliability axis: `HIGH | MED | LOW | NONE`. Empty `fact_ids` requires `NONE`; non-empty `fact_ids` requires non-`NONE` quality.

## Citation discipline — C1 to C4

1. **C1 — Fetch before cite.** Open and inspect the exact page before creating a fact. Search snippets, titles and memory are not evidence.
2. **C2 — Quote what you saw.** Each fact contains one short exact contiguous quote or table figure. Compact (`...`), spaced (`. . .`) and Unicode (`…`) ellipses are forbidden. Never splice separated passages.
3. **C3 — Attribute only what the URL establishes.** Never use a URL as support for a claim it does not establish. If no fetched source supports a background fact, omit the fact and label the selected value honestly as an estimate.
4. **C4 — Reopen and verify.** Before submission, enumerate and reopen every URL and verify its quote, attribution and all structured scope fields against the live page.

`evidence_facts[]` is the only place URLs may appear. Use stable unique IDs `F1`, `F2`, ... and reuse a fact by ID instead of duplicating its URL/quote. Every scope must state `population`, `geography`, `unit`, `period` and `industry_mismatch`. If a source omits any scope element, write exactly `not reported` for that field; never infer it silently.

## Frozen research rules

1. PE/M&A activity informs only B and M, never V or A.
2. Multiples are EV/EBITDA after market-rate owner compensation. State unit, target size, source population and every normalization. A normalization or market mapping that requires judgment is `ESTIMATE`.
3. A zero consolidator count is an absence-of-evidence estimate, not an observation. State the searches performed; do not cite a topical page as proving zero.
4. `current_adoption_pct` means the share of US firms in this NAICS materially using AI in automatable functions. Tool use, worker use and broad-sector use are different populations. Any judgmental mapping to the target population is `ESTIMATE`.
5. `t50_years` means years from today until 50% material firm adoption. If current adoption is already at least 50%, set it to `0`; otherwise it must be positive. Forecasting or analog-based bridging is normally `ESTIMATE`, even when supported by observed adoption facts.
6. Payer/regulatory repricing belongs in C when it determines who retains cost savings. Terminal class separately answers whether paid end demand survives mature AI; do not count the same clawback twice.
7. A statutory human-in-the-loop boundary affects terminal class only to the extent it preserves paid demand. It does not make automatable tasks non-automatable.
8. If a W-2 dataset labor share is distorted by 1099-heavy labor, use the injected value unchanged, flag `dataset_mismatch`, lower confidence as appropriate and show sensitivity. Do not silently adjust it.
9. Catch-all codes: identify at least the top three revenue subsegments, score the dominant subsegment, set `heterogeneous=true`, and complete the structured market-boundary disclosure.
10. `succession_shortage_documented` changes B. It is a full boolean selection under the same method/fact contract. `true` requires `OBSERVED`, at least one fact that directly documents a shortage, and exact live verification. A search-based `false` is normally `ESTIMATE` + `NONE`, with the searches and absence judgment fully disclosed in `estimate_basis`.

## Industry-specific guidance — hints, not facts

**Likely role structure:**
- **Technical delivery staff (developers, systems/deployment engineers)** — ~28-30% of wage bill (OEWS basis: 15-1252 Software Developers 23.9% + 15-1299 Computer Occupations All Other 3.1% + 15-1253 Software QA 2.2% at 4-digit 5415 — for this residual code the mix leans toward deployment/installation and recovery engineers rather than product developers); automatable: partial. Installation scripting, configuration, migration runbooks, DR plan documentation and testing automate heavily; on-site deployment hands, hardware handling and client-specific recovery orchestration judgment persist.
- **Support specialists** — ~6-9% of wage bill (OEWS basis: 15-1232 Computer User Support Specialists 3.3% at 4-digit; likely higher here given installation/support-service firms, est. ~6-9%); automatable: yes. Install support, troubleshooting and ticket work — directly AI-automatable except the physical/on-site slice.
- **Analysts and consultants** — ~7% of wage bill (OEWS basis: 15-1211 Computer Systems Analysts 4.6% + 13-1111 Management Analysts 2.7%); automatable: partial. Assessments, DR/continuity plans and audit documentation automate substantially; client governance persists.
- **Technical and project management** — ~11% of wage bill (OEWS basis: 11-3021 Computer & Information Systems Managers 8.3% + 13-1082 Project Management Specialists 3.0%); automatable: partial. Rollout coordination and status reporting automate; client escalation persists.
- **Company management and sales** — ~9-10% of wage bill (OEWS basis: 11-1021 General & Operations Managers 5.2% + 41-3091 Sales Reps 4.3%); automatable: partial. Owner-held client relationships persist; quoting and proposal work automates.
- **Admin and unallocated tail** — ~28-32% of wage bill (OEWS basis: remainder beyond the listed top-10 (top-10 sums to ~61% at 4-digit), est. ~28-32%); automatable: partial. Back office automates; part of the tail is field/bench technicians (physical) — allocate between patterns.

**Suggested sources:**
- (adoption) Kaseya Global MSP Benchmark Report / ConnectWise IT Nation research — Many firms coded 541519 operate as MSP-adjacent IT services shops; MSP-industry surveys are the closest adoption evidence, with the NAICS-frame caveat noted.
- (capture_pricing) Disaster-recovery-as-a-service (DRaaS) market coverage by Gartner/analyst firms — The DR sub-segment's pricing shift from consulting engagements to subscription DRaaS is the key capture/deflation fact if DR dominates the firm's mix.
- (consolidators_multiples) Solganick / Software Equity Group IT-services M&A reports — Lower-middle-market IT services deal multiples; firms from this residual code appear in the same deal pools as 541511/541512.
- (succession_owners) No dedicated owner-demographic source known for this code — Residual-code populations are not separately surveyed; fall back on ABS owner-age data or MSP-industry proxies, flagged as such. *(existence unverified — confirm this source still exists/publishes before relying on it)*

**Capture questions:**
- Firms in this residual code bill as T&M installation/deployment projects, per-incident or subscription DR/continuity fees, or resale-plus-services bundles — for the dominant sub-segment, when AI cuts delivery hours, does the billing form (hourly project work especially) pass savings straight to clients via competitive quotes, and is subscription DR revenue instead being absorbed by DRaaS platform vendors upstream?
- Who owns the client relationship — recurring-contract firms (DR retainers, support agreements) hold institutional relationships with switching costs, while project/installation shops rebid every job; what is the recurring vs. project revenue split in the target tier?
- What stops clients from using AI plus vendor-native tooling to self-serve — software installation is increasingly zero-touch by design (SaaS, autodeployment), and cloud platforms bundle DR natively; is the code's addressable base structurally shrinking regardless of firm-level AI adoption?
- Where in this heterogeneous code does durable accountability revenue exist (compliance-driven DR attestations, regulated-industry continuity requirements), and how large is it relative to the commodity installation work?

**Terminal-value question:**
This is a residual code whose two visible sub-segments face opposite mechanisms: installation/deployment services are being deleted by zero-touch software distribution (no AI needed — the product is disappearing by design), while disaster-recovery/continuity services migrate from paid human planning to platform-native DRaaS features. The question the run must answer per-sub-segment: is there any surviving paid service here that mature AI strengthens (accountable recovery orchestration for regulated clients) rather than replaces — and is the dominant revenue stream of the code's actual firms on the surviving or the disappearing side?

**Known traps:**
- Catch-all/residual code: identify the top-3 revenue sub-segments, score the DOMINANT one, and set heterogeneous: true. Hypothesis (flagged as such): general small IT services shops (MSP-adjacent, installation/support/resale-blended) dominate the firm count, with disaster recovery and specialized computer services as smaller slices — the modal firm here looks like a less-MRR-mature cousin of the 541512 MSP.
- Sibling boundaries: 541519 is what remains after 541511 (custom programming), 541512 (systems design/integration — where most self-described MSPs sit) and 541513 (client-facility operations) — NAICS assigns residual and mixed shops here, so the code's population is defined by classification noise; MSP benchmarks and deal data will not distinguish it from 541512, and any firm-level data should be checked for which sibling it really describes.
- labor_share 0.4958 signals meaningful pass-through (hardware/software resale) in receipts — normalize revenue mix before margin or multiple work, as with 541512.
- n_band 705 (20-99 class: 526 + 100-499: 179) — a real pool, but roll-up buyers (the MSP platforms) screen on MRR quality, and residual-code firms skew project/resale-heavy; expect the acquirable-on-MSP-terms subset to be a fraction of n_band.
- The 2022 receipts vintage predates AI service-desk tooling and the DRaaS mainstreaming — treat as a pre-shift baseline.

## Deterministic context — read-only

The finalizer injects these exact objects. Use them to form judgments, but do not copy them into the draft.

```json
{
  "labor_share": {
    "value": 0.4958,
    "method": "QCEW 2024 private total annual wages at 541519 ($19,543,540,823) x benefits multiplier 1.4294 [ECEC private-industry total comp $46.60/hr / wages $32.60/hr = 1.4294 (March 2026 reference period)] / SUSB 2022 receipts at 541519 ($42,029,844,000). Vintage mismatch: 2024 wages over 2022 receipts, stated. Level: 6-digit. Harmonized to IPS basis ÷1.3407 (median of 65 dual-method codes).",
    "sources": [
      "BLS QCEW 2024 annual averages (US000, private)",
      "BLS ECEC news release (raw/ecec.nr0.htm)",
      "Census SUSB 2022 (receipts by NAICS)"
    ],
    "year": "2024 wages / 2022 receipts",
    "quality": "MED"
  },
  "role_mix": {
    "oews_level": "4-digit 5415",
    "occupations": [
      {
        "soc": "15-1252",
        "title": "Software Developers",
        "emp_share": 0.2091,
        "wage_share": 0.2386
      },
      {
        "soc": "15-1232",
        "title": "Computer User Support Specialists",
        "emp_share": 0.0633,
        "wage_share": 0.033
      },
      {
        "soc": "11-3021",
        "title": "Computer and Information Systems Managers",
        "emp_share": 0.0533,
        "wage_share": 0.0827
      },
      {
        "soc": "15-1211",
        "title": "Computer Systems Analysts",
        "emp_share": 0.0481,
        "wage_share": 0.0455
      },
      {
        "soc": "41-3091",
        "title": "Sales Representatives of Services, Except Advertising, Insurance, Financial Services, and Travel",
        "emp_share": 0.0475,
        "wage_share": 0.043
      },
      {
        "soc": "11-1021",
        "title": "General and Operations Managers",
        "emp_share": 0.0351,
        "wage_share": 0.0518
      },
      {
        "soc": "15-1299",
        "title": "Computer Occupations, All Other",
        "emp_share": 0.0315,
        "wage_share": 0.0314
      },
      {
        "soc": "13-1082",
        "title": "Project Management Specialists",
        "emp_share": 0.029,
        "wage_share": 0.0304
      },
      {
        "soc": "13-1111",
        "title": "Management Analysts",
        "emp_share": 0.0268,
        "wage_share": 0.0273
      },
      {
        "soc": "15-1253",
        "title": "Software Quality Assurance Analysts and Testers",
        "emp_share": 0.0251,
        "wage_share": 0.0222
      }
    ],
    "quality": "MED"
  },
  "n_total": {
    "value": 10650,
    "source": "SUSB 2022 (firms, all enterprise sizes)",
    "year": "2022",
    "quality": "HIGH"
  },
  "n_band": {
    "value": 705,
    "derivation": "margin 0.1565 (margins.json[54]: Damodaran Jan 2026: Business & Consumer Services EBITDA/Sales 15.65%); revenue/employee $279,051 (SUSB 2022 receipts/employment at 541519); class <5: 8,483 firms, avg revenue/firm $429,327 (class receipts/firms), implied EBITDA $67,190 -> out of $1-10M band; class 5-9: 841 firms, avg revenue/firm $1,781,555 (class receipts/firms), implied EBITDA $278,813 -> out of $1-10M band; class 10-19: 505 firms, avg revenue/firm $3,678,558 (class receipts/firms), implied EBITDA $575,694 -> out of $1-10M band; class 20-99: 526 firms, avg revenue/firm $10,815,287 (class receipts/firms), implied EBITDA $1,692,592 -> IN $1-10M band; class 100-499: 179 firms, avg revenue/firm $40,627,151 (class receipts/firms), implied EBITDA $6,358,149 -> IN $1-10M band; class 500+: 116 firms, avg revenue/firm $190,265,517 (class receipts/firms), implied EBITDA $29,776,553 -> out of $1-10M band; n_band = 705",
    "margin_used": 0.1565,
    "margin_source": "Damodaran Jan 2026: Business & Consumer Services EBITDA/Sales 15.65%",
    "quality": "ESTIMATE"
  }
}
```

For `role_mix.occupations`, author one role judgment for every supplied `soc`, unchanged. The finalizer injects titles and wage shares. If listed wage shares sum below one, add exactly one `soc: "RESIDUAL"` judgment for the remainder. Role fractions are judgments: the finalized role aggregate is always an estimate. Never relabel or reinterpret a supplied SOC.

If `labor_share.value` or `n_band.value` is null, provide only that field in `dataset_fallbacks` under the same method contract. Otherwise the fallback is forbidden.

## Inputs to research and declare

- `current_adoption_pct`
- `historical_analogs`
- `owners_60plus_pct`, including structured succession evidence
- `active_consolidators`
- `buy_mult`
- `exit_mult`
- `pricing_structure`
- `within_role_automatable_fraction` for every supplied SOC and any required residual
- `pi_dist`
- `pi_moat`
- `t50_years`

Every selected input carries a rationale and plausible range. `t50_years` must appear in `top_uncertain_inputs`. Method and evidence reliability must be assessed independently.

## Frozen formulas — Python calculates them

- `V_raw = labor_share × ai_replaceable_share`
- `V = 10 × min(1, V_raw / 0.25)`
- `C = 10 × pi_dist × pi_moat`
- `A = 10` when `t50_years=0`, otherwise `min(10, 10 / t50_years)`
- `TD = clamp(log10(n_band) / 4, 0, 1)`
- `OW = clamp((owners_60plus_pct − 0.10) / 0.35, 0.1, 0.9)`; add 0.1 for documented succession shortage, cap at 1
- `CFD = min(0.9, log10(1 + active_consolidators) / 2)`
- `B = 10 × sqrt(TD × OW) / (1 + 0.3 × CFD)`
- `M = clamp(4 × (exit_mult − buy_mult) / buy_mult, 0, 10)`
- `S = (V × C × A × B × M)^(1/5)`

## Mandatory cross-check narratives

Answer all four candidly:

1. Uplift consistency, while acknowledging Python supplies final `uplift_pp`.
2. Terminal value: `DURABLE`, `PRESSURED` or `MELTING`, separating operator cost reduction from product replacement.
3. No-proxy confirmation that V/A exclude deal evidence.
4. Pricing-model deflation and its explicit mapping into `pi_dist`/`pi_moat`.

## Final self-audit — required before submission

1. Enumerate every `evidence_facts[].url`, reopen each URL and verify the exact contiguous quote, title, attribution and every scope field.
2. Enumerate every fact ID and confirm it is unique and referenced accurately.
3. For every selected input, verify its `fact_ids`, method, evidence quality, rationale and range.
4. For every `OBSERVED` selection, verify the cited fact states the selected value exactly.
5. For every `CALCULATED` selection, verify every operand against its fact, confirm the expression uses all operands, recompute it, and confirm there is no judgmental bridge.
6. For every judgmental bridge, require `ESTIMATE` even when background facts are cited.

## Output shape

Fill every abbreviated selection with the exact fields required by the schema. The example shows method-specific shapes; do not include fields belonging to another method branch.

```json
{
  "naics": "541519",
  "title": "Other Computer Related Services",
  "run_meta": {
    "model_id": "{{MODEL_ID}}",
    "run_date": "{{RUN_DATE}}",
    "run_id": "{{RUN_ID}}",
    "template_version": "3.1.1",
    "prompt_version": "{{PROMPT_VERSION}}"
  },
  "evidence_facts": [
    {
      "id": "F1",
      "url": "https://example.org/source",
      "title": "Exact source title",
      "quote": "One short exact contiguous excerpt or table figure",
      "access_date": "{{RUN_DATE}}",
      "scope": {
        "population": "not reported",
        "geography": "not reported",
        "unit": "not reported",
        "period": "not reported",
        "industry_mismatch": "not reported"
      }
    }
  ],
  "dataset_fallbacks": {},
  "inputs": {
    "ai_replaceable_share": {
      "role_judgments": [
        {
          "soc": "00-0000",
          "within_role_automatable_fraction": 0.0,
          "rationale": "",
          "plausible_range": ""
        }
      ],
      "evidence_quality": "NONE",
      "fact_ids": [],
      "estimate_basis": {
        "starting_fact_ids": [],
        "scope_mismatches": ["none; no source fact is used"],
        "bridge_steps": ["State the factual/search starting point and each bridge step."],
        "judgment_step": "State the explicit judgment used to select the value."
      }
    },
    "pi_dist": {
      "value": 0.0,
      "method": "ESTIMATE",
      "evidence_quality": "NONE",
      "fact_ids": [],
      "rationale": "",
      "plausible_range": "",
      "estimate_basis": {
        "starting_fact_ids": [],
        "scope_mismatches": ["none; no source fact is used"],
        "bridge_steps": ["State the factual/search starting point and each bridge step."],
        "judgment_step": "State the explicit judgment used to select the value."
      }
    },
    "pi_moat": {},
    "t50_years": {},
    "current_adoption_pct": {
      "value": 0.0,
      "method": "OBSERVED",
      "evidence_quality": "HIGH",
      "fact_ids": ["F1"],
      "rationale": "",
      "plausible_range": "",
      "observed_value": 0.0
    },
    "historical_analogs": {},
    "owners_60plus_pct": {
      "selection": {},
      "succession_shortage_documented": {
        "value": false,
        "method": "ESTIMATE",
        "evidence_quality": "NONE",
        "fact_ids": [],
        "rationale": "",
        "plausible_range": "false unless directly documented otherwise",
        "estimate_basis": {
          "starting_fact_ids": [],
          "scope_mismatches": ["none; no source fact is used"],
          "bridge_steps": ["List the searches performed and what they did not establish."],
          "judgment_step": "Treat the absence of located documentation as an estimate, not proof of absence."
        }
      }
    },
    "active_consolidators": {},
    "buy_mult": {},
    "exit_mult": {},
    "pricing_structure": {}
  },
  "cross_checks": {
    "uplift_vs_A_consistent": "",
    "terminal_value": {"class": "DURABLE", "justification": ""},
    "no_proxy_confirmed": "",
    "pricing_model_deflation": ""
  },
  "disclosures": {
    "dataset_mismatch": {"applies": false, "description": "", "sensitivity": []},
    "market_boundary": {
      "applies": false,
      "subsegments": [],
      "dominant_subsegment": "",
      "selection_basis": ""
    }
  },
  "confidence_overall": "LOW",
  "heterogeneous": false,
  "top_uncertain_inputs": [
    {"input": "t50_years", "plausible_range": "", "score_impact": ""}
  ],
  "reviewer_note": ""
}
```

URLs are permitted only in `evidence_facts[].url`. Output only the completed JSON object.
