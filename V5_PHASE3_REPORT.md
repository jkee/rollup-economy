# v5 Phase 3 fleet report

**Status:** complete on 2026-07-22. This report captures the Phase 3 gate before
the subsequently completed dashboard, documentation, and closure phases.

Victor approved the Phase 2 gate before the first fleet wave launched. The
remaining 58 codes were researched in independent sessions, finalized with the
frozen v5 toolchain, and reviewed in fresh isolated validator sessions. Together
with the five-code canary, the publication fleet contains 63 codes.

## Publication result

`python3 pipeline/v5/build.py site` completed fail-closed:

- Published: **63**
- Excluded: **0**
- Final review outcomes: **63 `publishable_with_caveats`**
- Review attempts: **65** — 63 `publishable_with_caveats`, 2 `invalid`
- Material findings across all attempts: **0**

The two invalid attempt-1 records, 541410 and 541420, had packet `run_id`
values with an erroneous `run` prefix. Their scores and memos reproduced, but
the identity gate correctly failed. Each received the single permitted fresh
attempt-2 research run and isolated review; both attempt-2 records were
`publishable_with_caveats`. No record required exclusion and no record was
retried more than once.

## Published distributions

| Dimension | Count |
|---|---:|
| Tier — HIGHEST_PRIORITY | 3 |
| Tier — PRIORITY | 12 |
| Tier — CONDITIONAL | 30 |
| Tier — LOW_PRIORITY | 11 |
| Tier — STRUCTURAL_OUT | 5 |
| Tier — unscored dataset gap | 2 |
| Readiness — MODEL_CONDITIONED | 61 |
| Readiness — STRESS_TEST_ONLY | 2 |
| Action — VALIDATE_ASSUMPTIONS | 61 |
| Action — EVIDENCE_FIRST | 2 |
| Robust tier present | 2 |

The two unscored records are 523940 and 541120; their frozen dataset inputs do
not support a base tier, so the deterministic gate assigns `STRESS_TEST_ONLY /
EVIDENCE_FIRST`. The two robust records are 541713 and 541922, whose interval
endpoints both remain `STRUCTURAL_OUT`.

## Integrity checks

- Every final packet passed `validate`, `finalize`, and `check`.
- Every review passed `build.validate_review` against its packet and reproduced
  mechanics block.
- The frozen sentinel suite passed **37/37**.
- `6digit/six_data_v5.json` was generated by the deterministic fail-closed site
  builder with zero exclusions or integrity errors.

No formula, anchor, cut, evidence rule, methodology contract, or frozen brief
was changed during the fleet run.
